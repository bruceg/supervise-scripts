#!/bin/sh
set -e
if [ "X$1" = X ]; then
	echo usage: svc-isdown service
	exit 1
fi
cd ${SVSCANDIR-/service}
PATH="$PATH:/usr/local/bin"
export PATH
svstat "$1" 2>/dev/null | fgrep -v ' up ' >/dev/null
