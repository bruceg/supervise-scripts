#!/bin/sh
set -e
if [ "X$1" = X ]; then
	echo usage: svc-isup service
	exit 1
fi
cd ${SVSCANDIR-/service}
PATH="$PATH:/usr/local/bin"
export PATH
svok "$1"
svstat "$1" 2>/dev/null | fgrep ' up ' >/dev/null
